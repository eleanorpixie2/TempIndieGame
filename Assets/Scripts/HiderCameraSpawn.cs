using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HiderCameraSpawn : MonoBehaviour {

	// Use this for initialization
	void Start ()
    {
        Debug.Log("Attached to: " + this.tag);
	}
	
	// Update is called once per frame
	void LateUpdate ()
    {
		
	}
}
